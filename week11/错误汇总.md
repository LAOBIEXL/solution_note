- min和max要求比较的两个参数的数据类型相同，如果是fabs等默认返回double类型的函数，那么需要强制类型转换；

- 不要在有多组输入的题目中全局初始化数组
![[Pasted image 20251123174853.png]]
```cpp
初始化方法：
#include<cstring
const int N = 110;
int a[N];

int main()
{
	int t;
	cin >> t;
	while(t--)
	{
		memset(a, 0, sizeof(a));
		速度会快很多；
	}

}

```


- 注意用于计数的循环变量的备份
![[Pasted image 20251123184144.png]]

- 关于坐标曲线，使用坐标加和的规律最快；
- 关于第一印象相悖的变量名，赶紧改掉；

- 关于解析式：
![[Pasted image 20251124001127.png]]

- 注意哈希冲突：
![[Pasted image 20251125205630.png]]

-  
![[Pasted image 20251125215643.png]]

- 注意vector开数组要加一：
![[Pasted image 20251125220615.png]]

- 一个总数为偶数的数组，中间的两个元素是n/2 -1 和 n/2;
奇数数组正中间的元素是n/2 ；
![[Pasted image 20251125224021.png]]

- 高精度算法多次出现去除前导零使用if语句而不是while语句的严重错误；

- 需要特别注意，==二进制枚举==当在最高位进行右移动并且按位异或的时候，会把不存在的一位置1，这种情况特别隐蔽，因为是通过二进制的一个数字存储，==所以不会有越界的提示，一定要小小心边界情况==；解决的办法如下：

- 注意重复类错误：
例如：使用哈希表注意有没有哈希冲突，给出的数据重复了会出现什么情况；
并且注意重复出现时值是否会产生叠加；

- ==注意，部分的给出条件不满足下标从零开始，需要人工平移坐标系，这个时候不要忘记给上界也加一==

- ![[Pasted image 20251203085228.png]]

 - 注意了，使用哈希表的时候，unordered_map对很多种情况没有提供哈希函数，所以只能使用map替代；或者缩略信息，比如将含有两个string的pair合并为一个string;
 ```cpp
   int n;
  cin >> n;
  int t = n;
  unordered_map<pair<string,string>, int> mp;
  while (n--)
  {
      string a, b;
      cin >> a >> b;
      a = a.substr(0, 2);
      if (a == b) continue;
      mp[{a,b}]++;
  }
  int ret = 0;
  for (auto e : mp)
  {
      int tmp = e.second;
      string a1 = e.first.first;
      string a2 = e.first.second;

  }
 ```

- 注意有多组相同解的情况，如何选择最早出现的解；
![[Pasted image 20251206111842.png]]

- 如图，不要1 到 n，或0-n-1一拍脑子就写了，要过一下脑子；
![[Pasted image 20251206190851.png]]

- dfs终止递归的条件； ![[Pasted image 20251207094940.png]]

- 边界条件防止错误：
  ```cpp
  列举法：
  1 <= k <= 1e5; 1 <= n <= 2e5;
  k = 1, n = 1;
  k = 5, n = 1; 
  k = 1; n = 5;
  k = 5, n = 5;
  ```

- 判断素数：
```cpp
防止越界写法：
for(int i = 2; i <= k / i; i++)
```
![[Pasted image 20251216093426.png]]

- dfs的返回出口位置：
return写到错误的位置是致命的；
![[Pasted image 20251216093530.png]]

- substr的用法；
![[Pasted image 20251218084041.png]]

- 分开取模会导致结果可能更大于模数，最后导致错误；可以移动取模运算；
![[Pasted image 20251221125922.png]]
![[Pasted image 20251221125940.png]]==上图错误==

- 用字符串存储dfs路径要注意不要包含超过十的节点，单个字符没办法表示10以上的数：
![[Pasted image 20251222185047.png]]

- 不要忘记矩阵边界：
![[Pasted image 20251222193440.png]]

- 在大量运算的情况下，除了要结果使用ll，在计算过程中也要开ll来进行输出