[[递归]]
## 分析：
问题入口，将最底下以上的盘子通过借助b移动到c后，在将最大的移动到b;

## 代码：
```cpp
void dfs(int n, char a, char b, char c)
{
	if(n == 0) return ;
	dfs(n - 1, a, b, c);
	printf("%c -> %c", a, b);
	dfs(n - 1, c, a, b);
}
```